{
  "name": "graphql_server",
  "version": "1.0.0",
  "description": "graphql backend of recipe application",
  "main": "server.ts",
  "author": "tbjag",
  "license": "MIT",
  "devDependencies": {
    "@pothos/plugin-prisma": "^3.47.2",
    "@types/node": "^18.16.1",
    "graphql-scalars": "^1.21.3",
    "graphql-yoga": "^3.9.1",
    "nodemon": "^2.0.22",
    "prisma": "^4.13.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.0.4"
  },
  "dependencies": {
    "@graphql-yoga/node": "^3.9.1",
    "@pothos/core": "^3.29.0",
    "@prisma/client": "^4.13.0",
    "graphql": "^16.6.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "ts-node-dev --transpile-only --no-notify --exit-child src/server.ts",
    "devmon": "ts-node-dev --transpile-only --no-notify --exit-child src/server.ts",
    "generate": "ts-node --transpile-only src/schema.ts",
    "seed": "ts-node --transpile-only src/test.ts"
  }
}
